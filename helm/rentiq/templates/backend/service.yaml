apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
    {{- include "rentiq.labels" . | nindent 4 }}
spec:
  selector:
    app: backend
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
