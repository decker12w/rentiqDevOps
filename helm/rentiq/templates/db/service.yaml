apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
    {{- include "rentiq.labels" . | nindent 4 }}
spec:
  selector:
    app: db
  ports:
    - port: {{ .Values.db.port }}
      targetPort: {{ .Values.db.port }}
